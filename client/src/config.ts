// Frontend configuration

// API URL - use environment variable or default to relative path for local development
export const API_URL = import.meta.env.VITE_API_URL || '/api';

// Other configuration variables can be added here
export const APP_NAME = 'Balance Sheet Tracker';
